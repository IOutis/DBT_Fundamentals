version: 2

sources:
  - name: dbt_m
    # This tells dbt to look at the PUBLIC Google project, not yours
    database: dbt-training-485209
    schema: dbt_mhussain
    tables:
      - name : patient_churn
